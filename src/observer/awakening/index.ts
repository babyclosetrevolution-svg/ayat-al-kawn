/**
 * Observer Awakening — the immersive initiation.
 *
 * Replaces the previous "Begin" cut-to-explorer with a gentle, multi-stage
 * lesson during which the Observer learns to glide through space. The
 * sequence is entirely self-contained: it owns its own R3F canvas, drives
 * the FlightController, and hands off cleanly to the main Universe.
 */

export { AwakeningOverlay } from "./AwakeningOverlay";
export { AwakeningState } from "./state";
export { AWAKENING_STAGES } from "./stages";
